name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  repository-safety:
    name: repository-safety
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify tracked env files
        run: |
          set -euo pipefail
          TRACKED_ENV="$(git ls-files | grep -E '(^|/)\.env(\.|$)' | grep -vE '\.env\.example$' || true)"
          if [ -n "$TRACKED_ENV" ]; then
            echo "ERROR: tracked .env files found (excluding .env.example):"
            echo "$TRACKED_ENV"
            exit 1
          fi
          echo "OK: no tracked .env secrets (.env.example allowed)"

      - name: Git status
        run: git status --short

  backend-go-check:
    name: backend-go-check (${{ matrix.module }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module:
          - packages/shared-go
          - services/api-gateway
          - services/billing-register-service
          - services/company-service
          - services/control-tower-read-model-service
          - services/document-service
          - services/identity-service
          - services/localization-service
          - services/rfx-service
          - services/shipment-service
          - services/transport-order-service
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: |
            packages/shared-go/go.sum
            services/**/go.sum

      - name: Go version
        run: go version

      - name: Run tests
        working-directory: ${{ matrix.module }}
        run: go test ./...

  rfx-deadline-worker-integration:
    name: rfx-deadline-worker-integration
    runs-on: ubuntu-latest
    services:
      postgres:
        image: mirror.gcr.io/library/postgres:16
        env:
          POSTGRES_USER: rfx_test
          POSTGRES_PASSWORD: rfx_test
          POSTGRES_DB: freight_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      CI: true
      TEST_DATABASE_URL: postgres://rfx_test:rfx_test@localhost:5432/freight_test?sslmode=disable
      REQUIRE_TEST_DATABASE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: |
            packages/shared-go/go.sum
            services/rfx-service/go.sum

      - name: Run deadline worker integration tests
        working-directory: services/rfx-service
        run: go test -tags=integration ./internal/integration/deadlineworker/... -count=1 -v

  rfx-enterprise-auth-audit-integration:
    name: rfx-enterprise-auth-audit-integration
    runs-on: ubuntu-latest
    services:
      postgres:
        image: mirror.gcr.io/library/postgres:16
        env:
          POSTGRES_USER: rfx_test
          POSTGRES_PASSWORD: rfx_test
          POSTGRES_DB: freight_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      CI: true
      TEST_DATABASE_URL: postgres://rfx_test:rfx_test@localhost:5432/freight_test?sslmode=disable
      REQUIRE_TEST_DATABASE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: |
            packages/shared-go/go.sum
            services/rfx-service/go.sum

      - name: Run enterprise auth/audit integration tests
        working-directory: services/rfx-service
        run: go test -tags=integration ./internal/integration/enterprise/... -count=1 -v

  rfx-carrier-portal-integration:
    name: rfx-carrier-portal-integration
    runs-on: ubuntu-latest
    services:
      postgres:
        image: mirror.gcr.io/library/postgres:16
        env:
          POSTGRES_USER: rfx_test
          POSTGRES_PASSWORD: rfx_test
          POSTGRES_DB: freight_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      CI: true
      TEST_DATABASE_URL: postgres://rfx_test:rfx_test@localhost:5432/freight_test?sslmode=disable
      REQUIRE_TEST_DATABASE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: |
            packages/shared-go/go.sum
            services/rfx-service/go.sum

      - name: Run carrier portal integration tests
        working-directory: services/rfx-service
        run: go test -tags=integration ./internal/integration/enterprise/... -count=1 -run 'Carrier' -v

  rfx-lane-ownership-integration:
    name: rfx-lane-ownership-integration
    runs-on: ubuntu-latest
    services:
      postgres:
        image: mirror.gcr.io/library/postgres:16
        env:
          POSTGRES_USER: rfx_test
          POSTGRES_PASSWORD: rfx_test
          POSTGRES_DB: freight_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      CI: true
      TEST_DATABASE_URL: postgres://rfx_test:rfx_test@localhost:5432/freight_test?sslmode=disable
      REQUIRE_TEST_DATABASE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: |
            packages/shared-go/go.sum
            services/rfx-service/go.sum

      - name: Run lane ownership integration tests
        working-directory: services/rfx-service
        run: go test -tags=integration ./internal/integration/laneownership/... -count=1 -v

  rfx-evaluation-award-integration:
    name: rfx-evaluation-award-integration
    runs-on: ubuntu-latest
    services:
      postgres:
        image: mirror.gcr.io/library/postgres:16
        env:
          POSTGRES_USER: rfx_test
          POSTGRES_PASSWORD: rfx_test
          POSTGRES_DB: freight_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      CI: true
      TEST_DATABASE_URL: postgres://rfx_test:rfx_test@localhost:5432/freight_test?sslmode=disable
      REQUIRE_TEST_DATABASE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: |
            packages/shared-go/go.sum
            services/rfx-service/go.sum

      - name: Run evaluation/award integration tests
        working-directory: services/rfx-service
        run: go test -tags=integration ./internal/integration/enterprise/... -count=1 -run 'Evaluation|Commercial|Award|Concurrent|Competitor|AuditFailure' -v

  rfx-award-to-transport-order-integration:
    name: rfx-award-to-transport-order-integration
    runs-on: ubuntu-latest
    services:
      postgres:
        image: mirror.gcr.io/library/postgres:16
        env:
          POSTGRES_USER: rfx_test
          POSTGRES_PASSWORD: rfx_test
          POSTGRES_DB: freight_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      CI: true
      TEST_DATABASE_URL: postgres://rfx_test:rfx_test@localhost:5432/freight_test?sslmode=disable
      REQUIRE_TEST_DATABASE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: |
            packages/shared-go/go.sum
            services/rfx-service/go.sum

      - name: Run award-to-transport-order integration tests
        working-directory: services/rfx-service
        run: go test -tags=integration ./internal/integration/enterprise/... -count=1 -run 'AwardedConversion|ConversionDenied|RepeatedConversion|ConcurrentConversion|MultiLotPrice|ConversionAudit|CompetitorDataIsolationAfterConversion|BuyerOwnAwarded|SameTenantForeign|CrossTenant|NonAwarded' -v

  order-execution-integration:
    name: order-execution-integration
    runs-on: ubuntu-latest
    services:
      postgres:
        image: mirror.gcr.io/library/postgres:16
        env:
          POSTGRES_USER: rfx_test
          POSTGRES_PASSWORD: rfx_test
          POSTGRES_DB: freight_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      CI: true
      TEST_DATABASE_URL: postgres://rfx_test:rfx_test@localhost:5432/freight_test?sslmode=disable
      REQUIRE_TEST_DATABASE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: |
            packages/shared-go/go.sum
            services/shipment-service/go.sum
            services/rfx-service/go.sum

      - name: Apply platform migrations
        run: |
          set -euo pipefail
          for file in infrastructure/migrations/*.up.sql; do
            psql "$TEST_DATABASE_URL" -v ON_ERROR_STOP=1 -f "$file"
          done

      - name: Run order execution integration tests
        working-directory: services/shipment-service
        run: go test -tags=integration ./internal/integration/orderexecution/... -count=1 -v

  frontend-web-admin-build:
    name: frontend-web-admin-build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web-admin
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/web-admin/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build web-admin
        run: npm run build

  frontend-web-procurement-check:
    name: frontend-web-procurement-check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck web-procurement
        run: pnpm --filter @freight-platform/web-procurement typecheck

      - name: Test web-procurement
        run: pnpm --filter @freight-platform/web-procurement test

      - name: Build web-procurement
        run: pnpm --filter @freight-platform/web-procurement build

  scripts-check:
    name: scripts-check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Shell syntax check
        run: |
          set -euo pipefail
          SCRIPTS=(
            tests/integration/smoke-test.sh
            tests/integration/run-upd-400-repro.sh
            tests/integration/upd-400-repro.sh
            scripts/dev/seed_dev_admin.sh
          )
          MISSING=()
          for script in "${SCRIPTS[@]}"; do
            if [ ! -f "$script" ]; then
              MISSING+=("$script")
              continue
            fi
            echo "Checking $script"
            bash -n "$script"
          done
          if [ "${#MISSING[@]}" -gt 0 ]; then
            echo "Missing scripts:"
            printf '  - %s\n' "${MISSING[@]}"
            exit 1
          fi
          echo "OK: all shell scripts present and syntax-valid"
